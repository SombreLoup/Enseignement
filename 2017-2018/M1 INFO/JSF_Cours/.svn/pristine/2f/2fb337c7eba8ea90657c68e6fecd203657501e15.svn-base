package dao;

import java.util.List;

import core.Concert;

public interface DAOConcert {
	Concert	get(int code);
	List<Concert>	loadAll();
	void	save(Concert concert);
	void 	update(Concert concert);
}
